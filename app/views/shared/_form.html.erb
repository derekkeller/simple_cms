<% if target.errors.any? %>
  <div class="error">
  	<%= pluralize(target.errors.size, "error") %> prevented this from properly saving:
    <ul>
		  <% target.errors.full_messages.each do |message| %>
			  <li><%= message %></li>
		  <% end %>
		</ul>
	</div>
<% end %>
